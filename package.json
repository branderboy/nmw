{
  "name": "nmw",
  "version": "0.1.0",
  "private": true,
  "description": "New Music Wednesdays — static funnel + admin",
  "scripts": {
    "dev": "http-server -p 5173 -c-1 -s .",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "@types/node": "^20.0.0",
    "http-server": "^14.1.1",
    "typescript": "^5.4.0"
  }
}
